info:
  name: Index
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/{{RESOURCE}}"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

runtime:
  variables:
    - name: RESOURCE
      value: deliveries

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
