info:
  name: Status Update
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{BASE_URL}}/{{RESOURCE}}/40a92a90-fb78-4eaa-bf72-7d0c77919594/status"
  body:
    type: json
    data: |-
      {
        "status": "shipped"
      }
  auth:
    type: bearer
    token: "{{token}}"

runtime:
  variables:
    - name: RESOURCE
      value: deliveries

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
