info:
  name: Status Update
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{BASE_URL}}/{{RESOURCE}}/a129de03-6576-4a21-9d21-4d4fd2436188/status"
  body:
    type: json
    data: |-
      {
        "status": "shipped"
      }
  auth:
    type: bearer
    token: "{{token}}"

runtime:
  variables:
    - name: RESOURCE
      value: deliveries

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
