info:
  name: Create
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/{{RESOURCE}}"
  body:
    type: json
    data: |-
      {
        "user_id": "7afcd84e-a2b1-48d2-bfba-00719278e68f",
        "description": "Monitor 8K"
      }
  auth:
    type: bearer
    token: "{{token}}"

runtime:
  variables:
    - name: RESOURCE
      value: deliveries

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
