info:
  name: Create
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/{{RESOURCE}}"
  auth: inherit

runtime:
  variables:
    - name: RESOURCE
      value: deliveries

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
